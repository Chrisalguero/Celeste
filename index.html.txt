<!DOCTYPE html>
<html lang="es">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <title>Feliz Cumple ❤️</title>

  <link href="https://fonts.googleapis.com/css2?family=Pacifico&display=swap" rel="stylesheet">

  <style>
    body {
      margin: 0;
      padding: 0;
      height: 100vh;
      background: radial-gradient(circle at 20% 30%, #1e2759, #0a0f29);
      animation: brilloFondo 6s ease-in-out infinite;
      overflow: hidden;
      display: flex;
      justify-content: center;
      align-items: center;
      font-family: "Poppins", sans-serif;
    }

    h1 {
      font-family: 'Pacifico', cursive;
      position: absolute;
      bottom: 70px;
      left: 50%;
      transform: translateX(-50%);
      text-align: center;
      width: 100%;
      font-size: 2.4rem;
      color: white;
      text-shadow: 0 0 10px rgba(0,0,0,0.4);
      padding: 0 10px;
    }

    /* Frases */
    .mensaje {
      position: absolute;
      top: -60px;
      left: 5vw;
      color: white;
      font-size: 1.1rem;
      opacity: 0.9;
      animation: caer 7s linear infinite;
      user-select: none;
      touch-action: none;
    }

    @keyframes caer {
      0% { transform: translateY(-60px); opacity: 1; }
      100% { transform: translateY(110vh); opacity: 0; }
    }

    /* Estrellas */
    .estrella {
      position: absolute;
      width: 3px;
      height: 3px;
      background: white;
      border-radius: 50%;
      opacity: 0.8;
      animation: parpadeo 2s infinite ease-in-out;
    }

    @keyframes parpadeo {
      0%, 100% { opacity: 0.3; }
      50% { opacity: 1; }
    }

    /* Luna */
    .luna {
      position: absolute;
      top: 80px;
      right: 60px;
      width: 140px;
      height: 140px;
      background: radial-gradient(circle at 30% 30%, #fff9e3, #d8d0a8 60%, #b8b088);
      border-radius: 50%;
      box-shadow: 0 0 40px rgba(255, 255, 220, 0.7), 0 0 80px rgba(255, 255, 200, 0.4);
    }

    @keyframes brilloFondo {
      0% { filter: brightness(1); }
      50% { filter: brightness(1.25); }
      100% { filter: brightness(1); }
    }
  </style>
</head>

<body>

  <h1>Feliz cumpleaños, hermosa 🎂✨</h1>

  <div class="luna"></div>

  <audio id="musica" src="musica.mp3" autoplay loop preload="auto"></audio>

  <script>
    /* Música después de interacción */
    function intentarReproducir() {
      const audio = document.getElementById('musica');
      audio.play().catch(() => {});
      document.removeEventListener('click', intentarReproducir);
      document.removeEventListener('touchstart', intentarReproducir);
    }

    document.addEventListener('click', intentarReproducir);
    document.addEventListener('touchstart', intentarReproducir);

    /* Estrellas de fondo */
    function crearEstrellas() {
      for (let i = 0; i < 120; i++) {
        const star = document.createElement('div');
        star.className = 'estrella';
        star.style.top = Math.random() * 100 + 'vh';
        star.style.left = Math.random() * 100 + 'vw';
        star.style.animationDelay = Math.random() * 2 + 's';
        document.body.appendChild(star);
      }
    }
    crearEstrellas();

    /* Frases */
    const mensajes = [
      "Espero que hoy tengas un día precioso ✨",
      "Que este nuevo año te traiga cosas bonitas",
      "Ojalá hoy te pasen cosas lindas 🌙",
      "Gracias por dar luz con tu vibra ✨",
      "Que tu sonrisa hoy brille más",
      "Te deseo lo mejor en tu día 🎉",
      "Que cumplas muchos más, con alegría 💫"
    ];

    function crearMensaje() {
      const msg = document.createElement("div");
      msg.className = "mensaje";
      msg.innerText = mensajes[Math.floor(Math.random() * mensajes.length)];
      msg.style.animationDuration = (7 + Math.random() * 3) + "s";
      document.body.appendChild(msg);
      setTimeout(() => msg.remove(), 11000);
    }

    setInterval(crearMensaje, 1600);

    /* Puedes mover los mensajes */
    document.addEventListener('pointerdown', (e) => {
      if (e.target.classList.contains('mensaje')) {
        const msg = e.target;
        let offsetX = e.clientX - msg.offsetLeft;
        let offsetY = e.clientY - msg.offsetTop;

        function mover(ev) {
          msg.style.top = ev.clientY - offsetY + 'px';
          msg.style.left = ev.clientX - offsetX + 'px';
        }

        function soltar() {
          document.removeEventListener('pointermove', mover);
          document.removeEventListener('pointerup', soltar);
        }

        document.addEventListener('pointermove', mover);
        document.addEventListener('pointerup', soltar);
      }
    });

    /* Efecto estela al deslizar */
    document.addEventListener('pointermove', (e) => {
      const spark = document.createElement('div');
      spark.className = 'estrella';
      spark.style.width = '5px';
      spark.style.height = '5px';
      spark.style.top = e.clientY + 'px';
      spark.style.left = e.clientX + 'px';
      spark.style.opacity = 1;
      spark.style.transition = 'transform 1s ease-out, opacity 1s';
      document.body.appendChild(spark);

      setTimeout(() => {
        spark.style.transform = 'translateY(-20px) scale(0)';
        spark.style.opacity = 0;
      }, 20);

      setTimeout(() => spark.remove(), 1000);
    });
  </script>

</body>
</html>
